<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spud.nimbus.nimbus_leaf.dao.LeafAllocMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.spud.nimbus.nimbus_leaf.model.LeafAlloc">
        <id column="biz_tag" property="bizTag" />
        <result column="max_id" property="maxId" />
        <result column="step" property="step" />
        <result column="update_time" property="updateTime" />
        <result column="description" property="description" />
        <result column="random_step" property="randomStep" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        biz_tag, max_id, step, update_time, description, random_step
    </sql>

</mapper>
